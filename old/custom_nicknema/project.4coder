version(2);
project_name = "custom_nicknema";

patterns = {
  "*.c",
  "*.h",
  "*.cpp",
  "*.bat",
  "*.sh",
  "*.4coder",
};

blacklist_patterns = {
  ".*",
};

load_paths_base = {
  { ".", .relative = true, .recursive = true, },
};

load_paths = {
  .win   = load_paths_base,
  .linux = load_paths_base,
  .mac   = load_paths_base,
};

commands = {
  .build = { .out = "*compilation*", .footer_panel = true, .save_dirty_files = true,
    .win   =   "build.bat",
    .linux = "./build.sh",
    .mac   = "./build.sh",
  },

  .build_lexers = { .out = "*lexers compilation*", .footer_panel = true, .save_dirty_files = true,
    .win   =   "build_lexers.bat",
    .linux = "./build_lexers.sh",
    .mac   = "./build_lexers.sh",
  },

  .check_statics = { .out = "*statics check*", .footer_panel = false, .save_dirty_files = false,
    .win   =   "check_statics.bat",
    .linux = "./check_statics.sh",
    .mac   = "./check_statics.sh",

  },

  .check_notes = { .out = "*notes check*", .footer_panel = false, .save_dirty_files = false,
    .win   =   "check_notes.bat",
    .linux = "./check_notes.sh",
    .mac   = "./check_notes.sh",
  },
};

fkey_command = {
  .F1 = "build",
  .F2 = "build_lexers",
  .F3 = "check_statics",
  .F4 = "check_notes",
};
